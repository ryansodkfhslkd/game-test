function setup()
{
	createCanvas(window.innerWidth, window.innerHeight);
	background(0, 0, 0);
}

function draw()
{
	
}

class box
{
	constructor(x,y, h, w)
	{
		this.x = x;
		this.y = y;
		this.h = h;
		this.w = w;
	}
}